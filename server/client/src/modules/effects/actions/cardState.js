const state = {
  changeCardState(options) {

  },
  changePower(options) {
    this.changeCardState(options);
  },
  changeLevel(options) {
    this.changeCardState(options);
  },
  changeLrigType(options) {
    this.changeCardState(options);
  },
  changeSigniType(options) {
    this.changeCardState(options);
  },
  changeColor(options) {
    this.changeCardState(options);
  },
  changeCard(options) {
    this.changeCardState(options);
  },
  negateEffects(options) {
    this.changeCardState(options);
  },
}

export default state;